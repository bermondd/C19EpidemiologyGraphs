# Example notebook
In order for `example_torch_geometric_notebook.ipynb` to work, the `C19EpidemiologyGraphs.sqlite3` database file
must be present in this folder and with that name exactly. If you already have it downloaded and decompressed,
a hard link is the easiest way to run the example without having to move the file back and forth.
